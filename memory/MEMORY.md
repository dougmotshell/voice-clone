# Memória do projeto

Índice de fatos que não se derivam do código nem do histórico do git. Uma linha
por entrada, detalhe no arquivo de tópico. Limite: 200 linhas.

O que **não** entra aqui: estrutura de diretórios, inventário de dependências,
decisão de arquitetura (isso é ADR), nem nada que um `grep` responda.

- [Desempenho medido](performance.md) — os números do laptop de referência e o que já foi tentado.

TODO: registrar o hardware de referência de cada colaborador, se o projeto
passar a ter mais de um.
